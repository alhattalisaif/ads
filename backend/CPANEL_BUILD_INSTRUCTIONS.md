Build cPanel ZIP workflow added.

Trigger this workflow from the Actions tab in GitHub (select the 'Build cPanel Deploy Zip' workflow, choose branch feature/mvp and click 'Run workflow').

The workflow will produce an artifact named `ads-cpanel-deploy` containing `ads-cpanel-deploy.zip`. Download it from the workflow run's 'Artifacts' section.
